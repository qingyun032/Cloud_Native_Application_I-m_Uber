export type passenger = {
    userName: string;
    isMale: boolean;
    // to be added
};

export type driver = {
    userName: string;
    isMale: boolean;
    // to be added
};

export type user = {
    password: string;
    username: string;
    email: string;
    phone: string;
    gender: string;
    home: string;
    company: string;
}
  